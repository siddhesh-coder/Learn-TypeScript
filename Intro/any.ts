let myName: string;

function getName() {
  return "Siddhesh";
}

myName = getName();

function checkReturn(value: number): number { //instead of using number use boolean so that this function will have boolean return.
  return true;
}

checkReturn(5);
